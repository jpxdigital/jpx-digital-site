node_modules/
dist/
.DS_Store
*.log

# Variáveis de ambiente e segredos — NUNCA commitar
.env
.env.*
!.env.example
.env.local
.env.deploy
KEYS.md
secrets.md
secrets/
*.pem
*.key
*.p12
*.pfx

# Next.js
.next/
out/

# Deploy — arquivos de ambiente
deploy/vm1/.env
deploy/vm2/.env
deploy/monitoring/.env
deploy/pdf-service/.env

# n8n workflows com credenciais embutidas
n8n-workflows/*.credentials.json

# Documentação interna — não entra no repo
docs/
deploy/evolution/.env
deploy/jas-bridge/.env

# Relatórios de auditoria de clientes — nunca commitar
Relatorio_Auditoria_*.docx
Relatorio_Auditoria_*.pdf
Relatorio_Auditoria_*.html

# Scripts operacionais de provisionamento — rodar localmente, não commitar
deploy/scripts/retry-a1flex.sh

# Backups locais do n8n
.n8n-backup-*/

# Claude Code — worktrees e memória local
.claude/
