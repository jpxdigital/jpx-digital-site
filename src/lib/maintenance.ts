// Modo manutenção — site fora do ar para o público, em reconstrução.
// Para tirar o site do ar: true. Para voltar ao normal: false.
// Alterar aqui e fazer push para main — nunca editar isso direto na VM.
export const MAINTENANCE_MODE = false
